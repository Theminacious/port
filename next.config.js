// next.config.js

/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "image.thum.io",
      "italiancollar.com",
      "shopfootball.in",
    ],
  },
};

module.exports = nextConfig;
